function home(){
    // window.open("form add .html")
    // window.location.href="form add .html"
    let h=window.innerHeight;
    let w=window.innerWidth;

    let c = window.event.clientX;
    let d= window.event.clientY;
    window.alert(h)
    window.alert(w)
    console.log(c,d)
}