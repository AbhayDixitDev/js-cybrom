// use strict
// static and dynamic typing language    in static we have to right data type first before variable and dynamic dont
// var let const
// data type number bigint (n)
// 7 primitive data types  
// number,bigint,string,boolean,undefined(treated as a error),null,symbol(makes unique);
// if we want to make a number bigint we add n in last of numbers value
// == for value comparison
// === for value and data type comparison
// event handling onclick, ondbclick

// object data types
// 1. object 2. array 3. function 4. class
// function types
// 1. normal 2. arrow function, 3. IIFE(immidiateky invoke function ) 4. higher order function
// 


//  DOM and BOM models 
// document object model and browser object model

// bom
// window.open


// lexical scope
// ++++++ use of global variable in local funtion or local scope

// variable hosting
//  it is only happens in var declared varible , when we declare any variable using var keyword it is declared on top of the scope